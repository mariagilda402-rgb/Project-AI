plugins {
    id 'com.android.application'
}

def mobileWebDir = file("../../mobile")

android {
    namespace 'com.nexus.mobile'
    compileSdk 34
    
    buildFeatures {
        buildConfig true
    }

    defaultConfig {
        applicationId "com.nexus.mobile"
        minSdk 23
        targetSdk 34
        versionCode 4
        versionName "1.3"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            applicationIdSuffix ""
            debuggable true
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation 'androidx.core:core:1.12.0'
}

tasks.register("copyMobileWebAssets", Copy) {
    from(mobileWebDir) {
        include "index.html", "app.js", "style.css", "manifest.json", "sw.js", "version.json"
    }
    into layout.projectDirectory.dir("src/main/assets")
}

preBuild.dependsOn(tasks.named("copyMobileWebAssets"))
